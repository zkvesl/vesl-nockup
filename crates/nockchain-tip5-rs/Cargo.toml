[package]
name = "nockchain-tip5-rs"
version = "0.1.0"
edition = "2024"
description = "Standalone tip5 Merkle tree — hash functions, tree construction, proof generation and verification with test vectors"
license = "MIT"

[dependencies]
nockchain-math = { git = "https://github.com/nockchain/nockchain.git" }
subtle = "2"
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
