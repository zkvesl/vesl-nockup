# Redirect: the old graft-intent demo moved

Before 2026-04-23, `templates/graft-intent/` held a custom-hash-gate demo that had nothing to do with intent coordination — it was using the `graft-intent` name for a gate-genericity example. That demo now lives at `templates/graft-hash-gate/`.

This directory now hosts the **family-5 intent-graft placeholder template** (see `README.md` alongside this file). Any `%intent-*` poke against the placeholder kernel crashes with `%intent-graft-placeholder` by design.

If an old link brought you here:
- Custom-hash-gate demo → `templates/graft-hash-gate/`
- Family-5 intent primitive (placeholder) → the rest of this directory
