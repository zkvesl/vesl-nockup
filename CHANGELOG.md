# Changelog

All notable changes to this workspace are documented here. The CLI tools (`nockup-graft`, `vesl-test`) and the shipped templates evolve in lockstep with mirrored copies of `vesl-core` and `vesl-wallet` crates — release notes call out which sibling SHAs a given vesl-nockup tag pins.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

> **Note.** Detailed changelog entries begin tracking with the next release after the public beta. Pre-beta history lives in git (`git log`) and in the cross-repo `docs/AUDIT_REPORT_FINAL_BETA_2026-05-25.md` in vesl-core.
